name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  checks:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - name: Verify formatting
        run: |
          if [ "$(gofmt -s -l . | wc -l)" -gt 0 ]; then
            echo "Unformatted files:"
            gofmt -s -l .
            exit 1
          fi
      - name: Verify go mod tidy
        run: |
          go mod tidy
          git diff --exit-code go.mod go.sum
      - run: go vet ./...

  security:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - name: Install tools
        run: |
          go install github.com/golangci/golangci-lint/v2/cmd/golangci-lint@v2.1.6
          go install golang.org/x/vuln/cmd/govulncheck@latest
      - name: Run golangci-lint
        run: golangci-lint run --timeout=5m ./...
      - name: Run govulncheck
        run: govulncheck ./...

  test-build:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - name: Test
        run: go test -race -coverprofile=coverage.out ./...
      # Uncomment after authorizing codecov.io for the repo:
      # - uses: codecov/codecov-action@v5
      #   with:
      #     files: coverage.out
      - name: Cross-compile
        run: |
          GOOS=linux  GOARCH=amd64 go build ./cmd/fmp
          GOOS=linux  GOARCH=arm64 go build ./cmd/fmp
          GOOS=darwin GOARCH=amd64 go build ./cmd/fmp
          GOOS=darwin GOARCH=arm64 go build ./cmd/fmp
